Ext.define('Taole.appManager.appVer.store.UpdateTypeStore', {
    extend: 'Ext.data.Store',
    model: 'Taole.appManager.appVer.model.SystemTypeItem',
    autoLoad: true,
    proxy: {
        type: 'ajax',
        url: APP_VER_UPDATE_TYPE,
        reader: {
            type: 'json'
        }
    }
});